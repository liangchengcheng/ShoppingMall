<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:background="#f0f0f0"
    android:layout_height="match_parent" >

    <android.support.v7.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:theme="@style/ToolbarTheme"
        app:popupTheme="@style/ThemeOverlay.AppCompat.Light"
        app:layout_collapseMode="pin" />

    <EditText
        android:id="@+id/sh"
        android:layout_width="fill_parent"
        android:layout_height="45dp"
        android:layout_below="@id/toolbar"
        android:layout_margin="10dip"
        android:background="@drawable/rectangle_write_bg"
        android:drawableLeft="@drawable/wb_search_icon"
        android:drawablePadding="5dp"
        android:hint="输入城市名或拼音"
        android:padding="10dip"
        android:textColor="#515151"
        android:textSize="18dp" >
    </EditText>

    <cxsjcj.hdsx.com.activity.shioce.LetterListView
        android:id="@+id/letterListView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/sh" >
    </cxsjcj.hdsx.com.activity.shioce.LetterListView>

</RelativeLayout>